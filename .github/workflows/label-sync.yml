name: Label Sync

on:
  pull_request:
    types: [opened, synchronize]

permissions:
  contents: read

jobs:
  sync-labels:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "syncing labels for pull requests"
